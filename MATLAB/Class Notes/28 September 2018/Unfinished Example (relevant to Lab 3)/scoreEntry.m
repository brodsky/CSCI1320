function [scoresVec, gradesVec, namesVec] = scoreEntry(numStudents)
% UNFINISHED EXAMPLE

% Preallocate some space:
scoresVec = zeros(1,numStudents);
gradesVec = blanks(1,numStudents);
namesVec = strings(1,numStudents);


end